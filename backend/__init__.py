"""Adaptive RAG Engine — backend package.

This package contains the full server-side implementation of the Adaptive
RAG system: configuration, the LangGraph orchestration core, RAG subsystems
(LLMs, embeddings, retrievers, rerankers, memory, loaders, indexing), the
FastAPI HTTP layer, and shared utilities.
"""
