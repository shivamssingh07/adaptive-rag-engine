# ==============================================================================
# Adaptive RAG Engine — Runtime Dependencies
# Python 3.12+
# Install with: pip install -r requirements.txt
#
# Versions below marked "verified" were installed and exercised directly
# against this codebase's test suite in development. Versions marked
# "minimum" (torch-dependent packages: sentence-transformers,
# langchain-huggingface) could not be pinned to an exact version verified
# in the development sandbox due to disk-space constraints on the torch
# wheel; a lower bound compatible with the verified langchain-core/pydantic
# versions is given instead, letting pip resolve a compatible current
# release.
# ==============================================================================

# --- Web framework (verified) --------------------------------------------------
fastapi==0.140.7
uvicorn[standard]==0.51.0
python-multipart==0.0.32

# --- Configuration & validation (verified) --------------------------------------
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2

# --- LangChain / LangGraph ecosystem (verified) ------------------------------------
langchain-core==1.5.2
langchain-community==0.4.2
langchain-groq==1.1.3
langchain-text-splitters==1.1.2
langgraph==1.2.10

# --- Embeddings & cross-encoder reranking (minimum; see note above) -----------------
langchain-huggingface>=0.1.2
sentence-transformers>=3.3.1
torch>=2.5.1

# --- Vector store (verified) ----------------------------------------------------
faiss-cpu==1.14.3

# --- Lexical retrieval (verified) ------------------------------------------------
rank-bm25==0.2.2

# --- Document parsing (verified) -------------------------------------------------
pymupdf==1.28.0
python-docx==1.2.0
pandas==3.0.2

# --- Optional web search fallback (verified) --------------------------------------
tavily-python==0.7.26

# --- Structured logging (verified) -------------------------------------------------
python-json-logger==4.1.0

# --- HTTP client (frontend api_client + tests) (verified) --------------------------
httpx==0.28.1

# --- Streamlit frontend ------------------------------------------------------------
streamlit>=1.41.1
