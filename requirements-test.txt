# ==============================================================================
# Adaptive RAG Engine — Lean CI/test dependencies
#
# Deliberately excludes torch, sentence-transformers, and
# langchain-huggingface: the test suite never loads a real embedding or
# reranker model (see docs/testing.md — fakes are injected at the same
# constructor seams the code was designed with), and
# tests/conftest.py automatically stubs those two packages' imports when
# they're genuinely not installed, so backend.rag.embeddings.* and
# backend.rag.rerankers.* still import cleanly.
#
# For full local development (including running the app against real
# models), use requirements-dev.txt instead, which includes everything in
# requirements.txt.
# ==============================================================================

fastapi==0.140.7
uvicorn[standard]==0.51.0
python-multipart==0.0.32
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2

langchain-core==1.5.2
langchain-community==0.4.2
langchain-groq==1.1.3
langchain-text-splitters==1.1.2
langgraph==1.2.10

rank-bm25==0.2.2
pymupdf==1.28.0
python-docx==1.2.0
pandas==3.0.2

tavily-python==0.7.26
python-json-logger==4.1.0
httpx==0.28.1

# faiss-cpu itself has no torch dependency and is small/fast to install —
# real FAISS index construction/search/persistence IS exercised for real
# in the test suite (only the embedding *model* is faked).
faiss-cpu==1.14.3

ruff==0.8.6
mypy==1.14.1
pytest==8.3.4
pytest-asyncio==0.25.2
pytest-cov==6.0.0
types-requests==2.32.0.20241016
